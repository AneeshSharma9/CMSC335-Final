<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather in <%= city %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
            color: white;
            background: <%=gradient %>;
            /* Dynamically set gradient */
            background-size: cover;
            background-attachment: fixed;
            transition: background 0.5s ease-in-out;
        }

        input {
            margin: 10px;
            padding: 10px;
            width: 200px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            padding: 10px 20px;
            background-color: white;
            color: #333;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #ccc;
        }

        hr {
            margin: 20px 0;
            border-color: rgba(255, 255, 255, 0.3);
        }

        table {
            margin: 0 auto;
            border-collapse: collapse;
            width: 80%;
            color: white;
        }

        th,
        td {
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 8px;
            text-align: center;
        }

        th {
            background-color: rgba(255, 255, 255, 0.2);
        }

        #animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .snow #animation::before,
        .snow #animation::after {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://www.animationsoftware7.com/img/agifs/snow02.gif') repeat center center / cover;
            z-index: 1;
            opacity: 0.4;
        }

        .rain #animation::before,
        .rain #animation::after {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://i.pinimg.com/originals/91/95/f4/9195f4dd1b69f90038f627c8af422429.gif') repeat center center / cover;
            z-index: 1;
            opacity: 0.1;
        }
    </style>
</head>

<body >
    <div id="animation"></div>
    <h1>Weather in <%= city %></h1>
    <form action="/weather" method="GET">
        <p>Enter a city/zipcode to check the weather:</p>
        <input type="text" name="location" placeholder="Enter city/zipcode" required>
        <button type="submit">Check Weather</button>
    </form>

    <h1><%= temperature %> °F<h1>
    <h2><%= description %><h2>

    <hr>
    <table>
        <thead>
            <tr>
                <th>Day</th>
                <th>Condition</th>
                <th>High</th>
                <th>Low</th>
            </tr>
        </thead>
        <tbody>
            <% for (let i=0; i < 7; i++) { %>
                <tr>
                    <td>Day <%= i + 1 %>
                    </td>
                    <td>Sunny</td>
                    <td>
                        <%= 20 + i %>°C
                    </td>
                    <td>
                        <%= 10 + i %>°C
                    </td>
                </tr>
                <% } %>
        </tbody>
    </table>

    <script>
        const description = "<%= description.toLowerCase() %>";
        
        if (description.includes("snow")) {
            document.body.classList.add("snow");
        } else if (description.includes("rain")) {
            document.body.classList.add("rain");
        }
    </script>
</body>

</html>